import { pageTitle } from 'ember-page-title';

<template>
  {{pageTitle "ember-remodal"}}

  <h1>ember-remodal</h1>
  <p class="docs-lede">A live tour of every ember-remodal 3.0 feature, with the
    source beside each example.</p>
</template>
